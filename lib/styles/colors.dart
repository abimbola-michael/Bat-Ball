import 'package:flutter/material.dart';

import '../utils.dart';

Color boardColor = Colors.white;
Color ballColor = Colors.yellow;
Color batColor = Colors.blue;
Color appColor = Colors.blue;

var appBackgroundColor = darkMode ? Colors.black : Colors.white;
var cardBackgroundColor = darkMode ? lighthestTintColor : Colors.white;


var tintColor = darkMode ? Colors.white : Colors.black;
var lightTintColor = darkMode ? Colors.white.withOpacity(0.8) : Colors.black.withOpacity(0.8);
var lightherTintColor = darkMode ? Colors.white.withOpacity(0.5) : Colors.black.withOpacity(0.5);
var lighthestTintColor = darkMode ? Colors.white.withOpacity(0.2) : Colors.black.withOpacity(0.2);
