enum Direction { up, down, left, right }
enum BallHitPoint{ x, y}

enum GameMode { unplayed,starting, playing, pause, end, goal }
enum GamePlatform { phone, online }
//enum GameType { oneonone, group}
//enum GameStyle { friendly, betting}

